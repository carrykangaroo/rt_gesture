# RT-Gesture 优化清单（第三轮评审）

> 评审日期：2026-03-07  
> 对比文档：需求规格说明书 v0.3 / 技术实现文档 v1.2  
> 测试状态：**58 passed, 1 skipped** (17 个测试文件)  
> 工作路径：`imporve-generic-neuromotor-interface/workspace/`

---

## 0. 评审摘要

### 上轮遗留项闭合情况

| 旧编号 | 描述 | 状态 |
|--------|------|------|
| O-01 | ConfigPanel 无设备选择器 | ✅ 已闭合：`device_combo` QComboBox (auto/cpu/cuda) |
| O-02 | GUI 无 ground-truth 可视化 | ✅ 已闭合：`ZmqReaderThread` 订阅 `gt_port`，`GestureDisplay.gt_list` |
| O-03 | 帧时间戳未对齐绝对 GT 时间 | ✅ 已闭合：`_stream_start_time` + `stream_start_time` header |
| O-04 | GUI 组件缺少测试 | ✅ 已闭合：`test_gui.py` 4 个测试 |
| O-05 | EmgPlotWidget 缺降采样优化 | ✅ 已闭合：`downsample_step` 基于 widget 像素宽度 |
| O-06 | Config 文件引用外部项目路径 | ✅ 已闭合：`checkpoint_path` 改为 `../checkpoints/` 相对路径 |
| O-07 | 数据中断多进程集成测试缺失 | ✅ 已闭合：`test_inference_recovers_after_data_timeout_gap()` |
| O-08 | 关闭后端口释放验证测试缺失 | ✅ 已闭合：`test_control_port_can_be_rebound_after_stop()` |
| O-09 | conftest.py CHECKPOINT_PATH 硬编码 | ✅ 已闭合：`_resolve_checkpoint_path()` 多候选路径 + env 变量 |
| O-10 | 概率热力图视图缺失 | ✅ 已闭合：`QTabWidget`(Bars / Heatmap) + viridis colormap |
| O-11 | 版本号管理缺失 | ✅ 已闭合：`__init__.py` 中 `__version__ = "0.2.0"` + `setup.py` 读取 |
| O-12 | README 缺架构图 | ✅ 已闭合：Mermaid flowchart + 模块/API/配置表 |
| O-13 | conv_history 文档补充 | ✅ 已闭合：技术文档 §2.4 已说明 `(1,16,0)` 初始化方案 |

**结论**：上轮 13 项可执行优化全部已闭合。

### 当前覆盖度概览

| 需求文档章节 | 对应功能 | 覆盖度 |
|-------------|---------|--------|
| F1 离线数据模拟器 | `data_simulator.py` | ✅ 完全覆盖 |
| F2 流式推理引擎 | `inference_engine.py` + `networks.py` | ✅ 完全覆盖 |
| F3 事件检测 | `event_detector.py` | ✅ 完全覆盖 |
| F4 事件输出 | 日志 + 回调 + ZMQ 发布 | ✅ 完全覆盖 |
| F5 桌面 GUI | `gui/` 6 个模块 | ✅ 完全覆盖 |
| F6 离线训练 | `train.py` + `data.py` + `transforms.py` | ✅ 完全覆盖 |
| F7 日志与存储 | `logger.py` | ✅ 完全覆盖 |
| §3.1 延迟 | 延迟打点 + 阈值告警 | ✅ 完全覆盖 |
| §3.2 跨平台 | Linux 完整 / Windows 信号兼容 | ⚠️ 未实测 Windows |
| §3.3 稳定性 | atexit + signal + cleanup | ✅ 完全覆盖 |
| §3.4 数据中断 | 超时 + seq gap → LSTM 重置 + warm-up | ✅ 完全覆盖 |
| §3.5 优雅关闭 | SHUTDOWN 级联 + timeout + kill | ✅ 完全覆盖 |
| §3.6 测试 | 58 tests / 17 files | ✅ 完全覆盖 |

---

## 1. 当前遗留优化项

### 本轮执行结果（N-01 ~ N-08）

| 编号 | 状态 | 落地结果 |
|------|------|----------|
| N-01 | ✅ 已闭合 | 新增 `test_cler_baseline_alignment_on_mini_window`（`@pytest.mark.slow`，`RT_GESTURE_RUN_SLOW=1` 启用） |
| N-02 | ✅ 已闭合 | 新增 `test_transforms.py`，覆盖 pulse 标签与 rotation 增强 |
| N-03 | ✅ 已闭合 | `test_cler_wrapper.py` 扩展至 6 个测试，覆盖 debounce/边界事件/对齐/误差行为 |
| N-04 | ✅ 已闭合 | `test_gui.py` 新增 MainWindow 集成测试（组件装配 + timer 初始化） |
| N-05 | ✅ 已闭合 | `CHANGELOG.md` 改为版本化条目并补充近期变更 |
| N-06 | ✅ 已闭合 | 技术文档回刷至 v1.2，补齐 config 字段、GUI 模块、测试矩阵与实现补充说明 |
| N-07 | ✅ 已闭合 | `__version__=0.2.0` 与 `CHANGELOG` 版本节 `[0.2.0]` 对齐 |
| N-08 | ✅ 已闭合 | 技术文档 §8 明确 `prompts` 为可选字段，缺失时回退空 DataFrame |

### P1 — 功能完整性

> 无 P1 级别的功能缺失。需求文档规定的所有功能点(F1–F7)及非功能需求(§3.1–§3.6)均已实现并有对应测试。

### P4 — 外部验证项（需结合环境/数据操作）

#### N-09 Windows + CUDA 环境实测

| 属性 | 值 |
|------|-----|
| 来源 | 需求 §3.2 |
| 现状 | 代码层面已做信号兼容处理（`if hasattr(signal, "SIGTERM")`），`mp.set_start_method("spawn")` 也适配 Windows，但**从未在 Windows 环境运行过** |
| 操作 | 在 Windows + CUDA 机器上安装依赖并运行完整测试 + 短时实时运行验证 |

#### N-10 分钟级稳定性运行验证

| 属性 | 值 |
|------|-----|
| 来源 | 需求 §3.3 |
| 现状 | 集成测试仅运行数秒（6 个 chunk） |
| 操作 | 使用 mini 或 full HDF5 进行 3–5 分钟连续运行，监控内存和延迟趋势 |

#### N-11 Full CLER 基准对齐（预训练 checkpoint + full 数据集）

| 属性 | 值 |
|------|-----|
| 来源 | 需求 §3.6 / 技术文档 §14.3 |
| 现状 | `evaluate.py` + `cler.py` 代码完备；当前开发机 CPU 上 full-sequence baseline forward 会触发内存上限，需外部算力环境执行 |
| 操作 | 运行 `python -m rt_gesture.evaluate --config config/evaluation.yaml` 并记录结果 |

#### N-12 250 epoch 正式训练

| 属性 | 值 |
|------|-----|
| 来源 | 需求 F6 / 技术文档 §13.2 |
| 现状 | `config/training.yaml` 配置就绪（`max_epochs: 250`），训练管线通过 `test_training.py` 验证可运行 |
| 操作 | 执行完整训练，验证产出 checkpoint 可被 `InferenceEngine` 加载并推理 |

---

## 2. 统计总结

| 级别 | 数量 | 编号 |
|------|------|------|
| P1 功能缺失 | 0 | — |
| P2 测试增强 | 0 | —（N-01~N-04 已闭合） |
| P3 文档/质量 | 0 | —（N-05~N-08 已闭合） |
| P4 外部验证 | 4 | N-09 ~ N-12 |
| **合计** | **4** | |

### 与上轮对比

| 指标 | 上轮（O-xx） | 本轮（N-xx） |
|------|-------------|-------------|
| P1 功能缺失 | 3 (O-01/02/10) | **0** |
| P2 测试增强 | 5 (O-04/07/08/03/06) | **0（已闭合）** |
| P3 文档/质量 | 5 (O-05/09/11/12/13) | **0（已闭合）** |
| P4 外部验证 | 4 | 4 |
| 测试数量 | 41 passed | **58 passed (+41%)** |
| 测试文件 | 12 | **17** |

**核心进展**：可在当前环境落地的优化项已全部闭合（O-01~O-13 + N-01~N-08）。剩余工作仅为外部环境验证项（N-09~N-12）。

---

## 3. 建议执行优先顺序

```
N-11 → N-10 → N-09 → N-12   (外部验证，按数据可得性排序)
```
